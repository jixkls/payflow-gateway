# Task 05 — Requisição assíncrona

A função `loadMerchant` não retorna os dados corretamente.

Investigue o fluxo assíncrono e corrija o problema.

## Critério de aceite

- função retorna os dados resolvidos;
- tipos permanecem corretos;
- não utilizar `.then()` apenas para contornar o problema;
- explicar no PR por que o código anterior estava incorreto.
