# Task 02 — TypeScript

Tipar corretamente a função `createMerchant`.

## Requisitos

- criar tipo para entrada;
- criar tipo para retorno;
- adicionar `phone` opcional;
- nenhum `any` explícito;
- nenhum `any` implícito;
- manter comportamento existente.
